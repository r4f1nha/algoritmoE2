#include <stdio.h>

int cubo(int x) {
    return x*x*x;
}
int main()
{
    int x;
    
    printf("Digite um valor: ");
    scanf("%d", &x);
    
    printf("O valor do cubo é: %d", cubo(x));
    
    return 0;
}
